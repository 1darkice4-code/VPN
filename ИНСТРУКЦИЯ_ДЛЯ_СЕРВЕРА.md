# 📝 Инструкция для обновления на сервере

## ✅ Быстрый способ (рекомендуется)

```bash
# 1. Обновите код
git pull origin main

# 2. Перезапустите бота
docker-compose restart vpn_bot

# 3. Проверьте логи
docker-compose logs -f vpn_bot
```

Бот будет работать в **демо-режиме** (генерирует конфиги без интеграции с WireGuard).

## ⚠️ Важно

wgREST не может напрямую управлять **linuxserver/wireguard** контейнером через API.

### Решения:

**Вариант 1: Демо-режим (текущий)**
- Бот генерирует конфиги
- Конфиги НЕ добавляются автоматически в WireGuard сервер
- Нужно вручную добавлять пиров

**Вариант 2: Ручное управление WireGuard**
Добавляйте конфиги пиров вручную в `/opt/vpn/wg/peer_name/`:
```bash
cd /opt/vpn/wg/peer_*/peer.conf
# Импортируйте конфиг в WireGuard
```

**Вариант 3: Использовать wgREST как автономный сервер**
Запустите wgREST БЕЗ linuxserver/wireguard и используйте его как основной WireGuard сервер.

## 🔧 Текущее состояние

- ✅ Telegram бот работает
- ✅ Бот генерирует конфиги
- ⚠️ Конфиги генерируются но не добавляются в WG автоматически
- ❌ wgREST не может управлять linuxserver/wireguard

## 📞 Что работает

1. **Telegram бот** - отвечает, сохраняет пользователей ✅
2. **Генерация конфигов** - работает (в демо-режиме) ✅
3. **База данных** - работает ✅
4. **WireGuard сервер** - работает ✅

## 🎯 Что нужно для продакшн

Чтобы VPN конфиги **полностью** работали:

1. Либо вручную добавляйте пиров в WireGuard
2. Либо используйте wgREST без linuxserver/wireguard
3. Либо установите WireGuard на хосте и используйте subprocess

---

**Система запущена, бот работает в демо-режиме!**

