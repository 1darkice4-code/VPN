# ⚡ Быстрое исправление - отправлено!

## ✅ Что исправлено

В логах wgREST видна ошибка:
```
"error":"code=400, message=missing key in request header"
```

Это означает, что **бот не отправляет токен авторизации** в запросах к wgREST.

### Исправлено:
✅ Добавлен `WGREST_AUTH_TOKEN` в переменные окружения бота  
✅ Бот теперь отправляет заголовок `Authorization: Bearer b1g5r4nd0mt0k3n12345`

---

## 📋 На сервере выполните:

```bash
# 1. Обновите код
git pull origin main

# 2. Перезапустите бота
docker-compose restart vpn_bot

# 3. Проверьте логи
docker-compose logs -f vpn_bot
```

### Должно быть:

```
✅ Конфиг получен через wgREST
```

Вместо:
```
❌ missing key in request header
```

---

## ✅ Проверка

```bash
# Проверьте что бот получает переменную
docker exec vpn_bot printenv WGREST_AUTH_TOKEN

# Должно вернуть: b1g5r4nd0mt0k3n12345
```

---

**Коммит**: `9ad9db1`  
**Исправление готово!**

